var c = document.getElementById("game");
var ctx = c.getContext("2d");
const width = ctx.width;
const height = ctx.height;
const cellWidth = 10;
const cellHeight = 10;

var grid = function(width, height, cellWidth, cellHeight) {
  return []
}
